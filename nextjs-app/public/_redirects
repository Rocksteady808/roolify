# Netlify redirects for Next.js static export

# API routes - NO redirects, pass through directly (! forces rule)
/api/submissions/webhook /api/submissions/webhook 200!
/api/* /api/:splat 200

# Static files
/_next/static/* /_next/static/:splat 200

# All other routes - serve index.html for client-side routing
/* /index.html 200
